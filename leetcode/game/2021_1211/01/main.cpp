#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<int> maxSubsequence(vector<int>& nums, int k) {
        vector<int> stack;
        for ()
    }
};

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}